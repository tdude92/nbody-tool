#ifndef NT_OCTREE_HPP
#define NT_OCTREE_HPP

#include <array>
#include <vector>

/**
 * @brief Node struct for octree.
 * 
 * @note Octants/cube vertices are represented in arrays in this order: +++, -++, --+, +-+, ++-, -+-, ---, +--
 */
struct BH_OctreeNode {
    // TODO BH_OctreeNode
};


class BH_Octree {
    // TODO BH_Octree class
    private:
    public:
};

#endif